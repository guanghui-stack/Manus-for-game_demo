// Binh Pháp Giấy Mực: route chính cố ý chỉ chứa GameCanvas, không đặt chrome React bên ngoài canvas.
import GameCanvas from "@/components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}
